# Copilot Instructions
# 此文件从 RULES.md 自动生成，请勿手动修改。

请严格遵守项目根目录 `RULES.md` 中的所有规则。

关键规则摘要：
1. 只修改当前 Issue 所属模块的目录，不能越界
2. 禁止直接 push 到 main 分支
3. 禁止引入新的 npm 依赖（需要时通知管理者）
4. 共享层文件修改需要特别标注
5. 一个 Issue = 一个分支 = 一个 PR

工作流脚本位于 `scripts/workflow/`，通过自然语言触发。
