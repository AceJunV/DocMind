# 6.15 优化变更文档

## 一、WPS 格式支持

新增 `.wps`（WPS 文字文档）的上传与解析支持。

| 修改文件 | 修改内容 |
|---------|---------|
| `types/index.ts` | `file_type` 联合类型新增 `'wps'` |
| `services/documentParser.ts` | `SUPPORTED_EXTENSIONS` 添加 `.wps`；`parseDocument` switch 新增 `case 'wps'`；新增 `parseWps` 函数（ZIP/OLE2/纯文本三重检测，复用 `parseDoc` 相同逻辑） |
| `components/DocumentUploadModal.tsx` | `accept` 属性、toast 提示、badge 区均添加 WPS |
| `pages/DashboardPage.tsx` | 两处说明文字添加 WPS |
| `pages/documents/DocumentListPage.tsx` | `FILE_ICONS` 和 `FILE_COLORS` 添加 wps（橙色） |
| `pages/documents/DocumentDetailPage.tsx` | `FILE_ICON_MAP` 和 `FILE_COLOR_MAP` 添加 wps（橙色） |

## 二、邀请角色逻辑修复

### 2.1 邀请列表数据源修正

**问题：** `review.agents` 为空时，旧代码回退到 `allAgents`（全量角色仓库），但模板角色（`tpl-xxx`）不在其中，导致无法邀请。

**修复：** 将邀请列表的数据源改为与"新建研讨室"（`ChatRoomConfigModal`）一致的三级合并：

```
review.agents（有则纳入）→ allAgents（visibleInReview !== false）→ templates（templateVisibility[id] !== false，解析为完整 Agent）
→ Map 按 id 去重 → 排除 room.participants
```

### 2.2 邀请按钮位置调整

- 从顶部栏移除独立的 `[邀请角色]` 按钮
- 移到左侧参与者卡片，与"参与者"标题同行右侧，按钮文字为"邀请"
- 参与者列表限定 `max-h-[260px]`，超出滚动（约 6 个头像）

### 2.3 新增引用

- `frontend/src/stores/agentStore` 新增引用：`buildSystemPrompt`、`templateVisibility`

## 三、讨论总结组件增强

`DiscussionSummaryCard` 新增 `error` 属性和重试 UI：

- 新增 `error?: string | null` prop
- 有 error 时显示红色错误文案 + "重新生成"按钮
- 关闭或重新生成时清除错误状态

## 四、议题消息作用域

- 新增 `ChatMessage.topicId?: string` 字段
- `visibleMessages` 改为严格按 `currentTopic.id` 过滤（`message.topicId === currentTopic.id`），旧的无 topicId 消息在选定议题时隐藏
- `handleSend` 构建 LLM 上下文时只传入当前议题的消息
- `addAgentMsg` 接受 `topicIdOverride` 参数，避免闭包时机问题
- 议题切换依赖数组新增 `currentTopic`，切换时自动滚动到底部
- 议题切换时仅保留一条过渡系统消息（去除重复的第二条）
- 总结生成按当前议题过滤消息

## 五、追问按钮与路由

- "追问"改为蓝色文字按钮，置于气泡右侧外部，hover 可见，顶部对齐
- 点击追问预填 `@角色名`，清空用户输入，显示回复目标条
- 回复目标条中的引用内容去除 `@提及` 显示
- 用户未输入 `@` 时，以被回复消息的发送者为目标 agent 路由
- `parseTargetAgent` 去除 `^` 锚点，支持输入框中任意位置 `@`
- 发送时计算 `effectiveTarget` 作为 `target_agent_id` 和 `runDiscussionRound` 的目标

## 六、Agent 回复优化

- `buildChatSystemPrompt` 新增 `userName` 参数，注入 `当直接回应用户 {name} 时，请在开头使用 @{name}`
- `DiscussionTurn` 新增 `isUserDirected` 字段
- `buildTurnInstruction` 在 `isUserDirected && userName` 时生成 `请直接回应用户 {userName} 刚才的提问`

## 涉及文件清单

```
frontend/src/types/index.ts
frontend/src/services/documentParser.ts
frontend/src/components/DocumentUploadModal.tsx
frontend/src/components/chat/DiscussionSummaryCard.tsx
frontend/src/pages/DashboardPage.tsx
frontend/src/pages/chat/ChatRoomPage.tsx
frontend/src/pages/documents/DocumentDetailPage.tsx
frontend/src/pages/documents/DocumentListPage.tsx
```
