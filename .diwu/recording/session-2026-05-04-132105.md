## Session 2026-05-04 13:21:05

### Task#4: 评审入口与流程优化 → Done
**实施内容**:
- 在 `frontend/src/pages/reviews/ReviewCreatePage.tsx` 的单文档模式中新增“案例详情”展示区，点击文档后展示标题、字数、类型、摘要和正文预览。
- 在案例详情区底部新增醒目的大号“开始评审”按钮，沿用 `canStart` 与 `handleStart`，不增加额外预览确认门槛。
- 保留并验证 Agent 选择区“全选/清空”能力。
- 扩展 `frontend/e2e/done-tasks-smoke.spec.ts`，覆盖单文档案例详情、对比评审输入、对比报告筛选、角色人物设定工具入口。

**验收验证**:
- [x] Given 用户点击评审案例 When 进入案例详情页 Then 先展示案例详情内容，且下方放置醒目的大“提交评审”按钮（L1: Playwright `done-tasks-smoke.spec.ts` 通过；L3: build/lint/unit test 通过）
- [x] Given 用户在 Agent 选择区 When 查看 Agent 列表 Then 存在“全选”按钮，点击后 Agent 被选中（L1: Playwright `done-tasks-smoke.spec.ts` 通过）
- [x] Given 用户已上传文档 When 发起评审 Then 不需要再次预览文档即可直接提交（L2: `canStart` 仅依赖文档、角色、模型配置；详情区按钮直接调用 `handleStart`）

**提交**: 待提交

### 下一步: dloop 第 2 轮选择 Task#5

### 本次踩坑/经验

- [环境漂移] 5173 端口运行的不是当前前端项目 → 该服务配置了 `/jiaoyan/` base，导致 E2E 命中错误应用 → 误判为路由或页面加载问题 → 应先确认目标端口对应的实际 dev server，本轮改用当前项目的 `http://127.0.0.1:5180` 验证。
