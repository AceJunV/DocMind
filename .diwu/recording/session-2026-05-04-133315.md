## Session 2026-05-04 13:33:15

### Task#6: 评审报告增强（核心提炼+证据跳转+导出） → Done
**实施内容**:
- 在 `frontend/src/pages/reviews/ReviewDetailPage.tsx` 新增可折叠“核心提炼”模块，展示 3 个核心问题和 2-3 个保留亮点。
- 建议证据与维度证据改为可点击证据入口，点击后滚动到“原文证据定位”区，并显示右下角证据高亮浮层和返回入口。
- 新增“导出图片”按钮，使用 Canvas 生成 PNG 报告，包含标题、综合评分、总体诊断、维度雷达图、维度均值、核心问题和优先建议。
- 扩展 `frontend/e2e/done-tasks-smoke.spec.ts`，覆盖核心提炼、证据定位、证据浮层和图片下载。

**验收验证**:
- [x] Given 评审报告已生成 When 查看报告顶部 Then 显示核心提炼模块且可收起/展开，包含核心问题和亮点（L1: Playwright 报告页断言通过）
- [x] Given 报告中有证据字段 When 点击证据文本 Then 页面滚动到原文证据定位区并显示证据高亮浮层，浮层可返回报告/查看原文（L1: Playwright 断言通过）
- [x] Given 用户在报告页 When 点击导出图片 Then 下载 PNG 报告，包含雷达图、维度评分、建议等关键信息（L1: Playwright download 断言通过；L2: Canvas 绘制链路命中）
- [x] `npm run lint`、`npm run build`、`npm test -- --run` 全部通过（L3）

**提交**: 待提交

### 下一步: dloop 第 4 轮选择 Task#7

### 本次踩坑/经验

- [验证误读] React Hooks lint 报 `useMemo` 条件调用 → 新增 hook 放在了 `review` 早返回之后，导致不同渲染路径 hook 顺序不稳定 → 误判为普通 memo 依赖问题 → 应把该派生值改为普通同步计算，或确保所有 hooks 都在早返回之前声明。
