## Session 2026-05-04 13:27:31

### Task#5: 角色引导与聊天室配置流程 → Done
**实施内容**:
- 在 `frontend/src/pages/reviews/ReviewCreatePage.tsx` 的无角色状态新增明确引导，提供“创建角色”入口，避免用户停留在空列表。
- 在 `frontend/src/pages/chat/ChatListPage.tsx` 中将评审报告进入研讨的流程从“自动建房并跳转”改为先打开“配置教研研讨”弹窗。
- 评审研讨配置会预填讨论主题、参与角色与引导式讨论模式，并保留上下文深度、主动性、冲突强度、产品气质、过程反馈等策略参数；用户点击创建后才进入聊天室。
- 扩展 `frontend/e2e/done-tasks-smoke.spec.ts`，覆盖评审进入聊天室前配置弹窗和无角色创建引导。

**验收验证**:
- [x] Given 用户发起评审且尚未创建任何角色 When 进入 Agent 选择 Then 显示明确角色新建引导和创建按钮（L1: Playwright `review create guides users when no agents exist` 通过）
- [x] Given 用户已选择 Agent 并跳转聊天室 When 进入聊天室前 Then 弹出配置弹窗且必须先完成配置再进入（L1: Playwright `compare create flow, compare report, and agent persona tools` 通过）
- [x] 配置参数写入聊天室 `strategy` 并用于后续讨论链路（L2: `handleCreateRoom` 写入 `discussionMode`、`contextDepth`、`initiativeLevel`、`conflictLevel`、`roomTone`、`feedbackLevel`）
- [x] `npm run lint`、`npm run build`、`npm test -- --run` 全部通过（L3）

**提交**: 待提交

### 下一步: dloop 第 3 轮选择 Task#6

### 本次踩坑/经验

- [验证误读] 在同一 Playwright 页面内直接改 localStorage 后仍看到旧角色 → Zustand store 已在内存中持有旧状态，路由跳转不等于重新初始化 store → 误判为无角色 UI 未生效 → 应在页面加载前用 `addInitScript` 注入空角色状态，或使用独立测试上下文验证持久化初始态。
